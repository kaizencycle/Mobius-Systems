# Integrity Pulse Environment Configuration
# Copy this file to .env.local and customize

# Data source mode (set to 'false' to use real WebSocket data)
NEXT_PUBLIC_MOCK_DATA=true

# WebSocket URL for real-time sentinel metrics
# Default: ws://localhost:4001/ws/gi-stream
NEXT_PUBLIC_WS_URL=ws://localhost:4001/ws/gi-stream

# Alternative: HTTP polling endpoint (if WebSocket not available)
# NEXT_PUBLIC_POLL_URL=http://localhost:4001/api/sentinels/metrics
# NEXT_PUBLIC_POLL_INTERVAL=2000

# Development server port
# Default: 3010
PORT=3010
